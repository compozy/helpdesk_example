{
  "name": "helpdesk-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "bun@1.3.11",
  "scripts": {
    "typecheck": "bun run --filter backend typecheck && bun run --filter frontend typecheck && bun run --filter helpdesk-cli typecheck"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^6.0.2"
  },
  "overrides": {
    "vite": "8.0.8"
  }
}
